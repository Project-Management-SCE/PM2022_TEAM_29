pipeline { 
agent any 
    stages { 
      stage('Build') {
            agent {
                docker {
                    image 'simagix/google-map-api:latest'
                    reuseNode true
                }
            }
            steps {
                sh 'gradle --version'
            }
        stage ('Test') { 
        
        }
        stage ('QA') { 
        
        }
        stage ('Deploy') { 
        
        }
        stage ('Monitor') { 
 
        }
 
    }           
 }
}
